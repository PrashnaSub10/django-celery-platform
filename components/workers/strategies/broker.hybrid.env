# BROKER_MODE is read by up.sh to select this strategy file.
# It is NOT injected into containers — it is a launcher-only variable.
BROKER_MODE=hybrid

# WHY: Separating fast and critical workloads prevents slow tasks
# from blocking high-throughput queues.
# Use when: full enterprise scale, mixed workloads (fast + critical).
