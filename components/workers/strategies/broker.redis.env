# BROKER_MODE is read by up.sh to select this strategy file.
# It is NOT injected into containers — it is a launcher-only variable.
BROKER_MODE=redis

# WHY: Redis is fast but not durable under memory pressure.
# Tasks may be lost if eviction occurs or Redis restarts.
# Use when: real-time notifications, cache invalidation, non-critical async.
