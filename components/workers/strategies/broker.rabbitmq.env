# BROKER_MODE is read by up.sh to select this strategy file.
# It is NOT injected into containers — it is a launcher-only variable.
BROKER_MODE=rabbitmq

# WHY: RabbitMQ persists messages and supports acknowledgements.
# Tasks are re-queued if workers crash.
# Use when: payments, emails, audits, mission-critical processes.
