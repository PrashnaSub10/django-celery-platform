# ============================================================
# requirements/pdf.txt — PDF generation & image processing
# ============================================================
# Paired with Dockerfile.pdf (which installs MuPDF, Pango, etc).
# Only install in workers that generate PDFs or process images.
# ============================================================

# Pins below must ship cp313 wheels — the base image is Python 3.13.
# Older pins (PyMuPDF 1.23.x, Pillow 10.2) have no 3.13 wheels and force a
# from-source MuPDF build that fails on modern g++.
PyMuPDF==1.26.7
Pillow==11.3.0
pillow-heif==1.4.0
reportlab==4.0.7
pypdf==6.7.0
weasyprint==63.1
xhtml2pdf==0.2.16
arabic-reshaper==3.0.0
python-bidi==0.4.2
svglib==1.5.1
